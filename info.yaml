# Tiny Tapeout project information
project:
  title:        "ChaCha20"      # Project title
  author:       "Raphael Eguchi"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "RFC 8439 ChaCha20 encrypt/decrypt core with a UART host interface"      # One line description of what your project does
  language:     "SystemVerilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     23000000       # 23 MHz; BAUD_DIV = 23e6 / 115200 ≈ 200 (clean ss sign-off; must match the board clock)

  # How many tiles your design occupies? A single tile is about 340x160 uM.
  # Starting generous (3x4) for the first hardening run; right-size from the
  # utilization report (design is ~2000 FFs + a 4-wide ARX datapath).
  tiles: "4x2"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 3x4 or 4x4

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_egurapha_chacha20"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "tt_um_egurapha_chacha20.sv"
    - "chacha20_controller.sv"
    - "chacha20_core.sv"
    - "quarter_round.sv"
    - "adder32.sv"
    - "uart_rx.sv"
    - "uart_tx.sv"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: ""
  ui[1]: ""
  ui[2]: ""
  ui[3]: "RX"
  ui[4]: ""
  ui[5]: ""
  ui[6]: ""
  ui[7]: ""

  # Outputs
  uo[0]: "BUSY"
  uo[1]: "ERR"
  uo[2]: ""
  uo[3]: ""
  uo[4]: "TX"
  uo[5]: ""
  uo[6]: ""
  uo[7]: ""

  # Bidirectional pins
  uio[0]: ""
  uio[1]: ""
  uio[2]: ""
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
